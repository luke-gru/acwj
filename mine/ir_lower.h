//typedef enum AsmOp {
    //ASM_MOVE_REG_REG, // movq, movl, movzbl (see cgreturn)
    //ASM_MOVE_MEM_REG, // memory to register: movzbq, movslq, movq (see cgloadlocal)
    //ASM_MOVE_REG_MEM, // register to memory: movb, movl, movq (see cgstorlocal)
    //ASM_LOAD_IMM, // movq, (see cgloadint)
    //ASM_JUMP,
//} AsmOp;

//typedef struct AsmNode {
    //AsmOp op;
    //Sym *sym; // for type info
    //int reg_src; // register number
    //int reg_dst; // register number
    //int vreg_src;
    //int vreg_dst;
    //int real_regs;
    //int labelnum; // for jumps
    //AsmNode *next;
//} AsmNode;
